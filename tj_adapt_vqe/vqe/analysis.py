def analysis():
    """we run this after running the pools 
    1. recreate the molecule
    2. load the data u saved
    3. make the data look pretty so u can analyze it 
        i.e. create list of (operators, coefficients)
    4. start w/the hartree fock state
    5. apply ops to hf state 
    6. get final wavefunction 
    """
    for i in range(2):
        print("hello") 
analysis()